version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - bodyclose
    - noctx
    - exhaustive

issues:
  exclude-dirs:
    - api/gen
  max-issues-per-linter: 0
  max-same-issues: 0
